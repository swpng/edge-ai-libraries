name: "[DLS] [U22/24] Build and test .deb pkgs & deb imgs"
run-name: "[DLS] [U22/24] Build and test .deb pkgs & deb imgs (by ${{ github.actor }})"
on:
  workflow_call:
  workflow_dispatch:
permissions: {}
env:
  dlstreamer-version: "2025.0.1.3"
  MODELS_PATH: $HOME/models
  VIDEO_INPUTS_PATH: $HOME/videos
  DLS_PATH: ./edge-ai-libraries-repo/libraries/dl-streamer

jobs:
  build:
    name: Build and test ${{ matrix.ubuntu_version }} .debs & deb imgs
    runs-on: ${{ matrix.runner_labels }}
    permissions:
      contents: read
      packages: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - path_deb: $DLS_PATH /docker/onebinary/ubuntu22/dlstreamer_deb_package_ubuntu22.Dockerfile
            path_dev: $DLS_PATH /docker/devel/ubuntu22/dlstreamer_dev_ubuntu22.Dockerfile
            ubuntu_version: ubuntu22
            runner_labels: [ubuntu-latest] # Build and run tests on Tiger Lake system with Ubuntu 22
          - path_deb: $DLS_PATH /docker/onebinary/ubuntu24/dlstreamer_deb_package_ubuntu24.Dockerfile
            path_dev: $DLS_PATH /docker/devel/ubuntu24/dlstreamer_dev_ubuntu24.Dockerfile
            ubuntu_version: ubuntu24
            runner_labels: [ubuntu-latest] # Build and run tests on Tiger Lake system with Ubuntu 24
          - path_deb: $DLS_PATH /docker/onebinary/ubuntu24/dlstreamer_deb_package_ubuntu24.Dockerfile
            path_dev: $DLS_PATH /docker/devel/ubuntu24/dlstreamer_dev_ubuntu24.Dockerfile
            ubuntu_version: ubuntu24
            runner_labels: [ubuntu-latest] # Build and run tests on Arrow Lake system with Ubuntu 24
    steps:
    - name: Check out edge-ai-libraries repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 #4.2.2
      with:
        persist-credentials: false
        path: edge-ai-libraries-repo

    - name: Init submodules
      run: |
        cd edge-ai-libraries-repo
        git submodule update --init libraries/dl-streamer/thirdparty/spdlog

    # ======================================================== BUILDING PART ========================================================
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@b5ca514318bd6ebac0fb2aedd5d36ec1b5c232a2 #3.10.0

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 #3.4.0
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build dev release img with cache from GHCR
      env:
        dev_release_img: ghcr.io/${{ github.repository }}/dev-release-img-${{ matrix.ubuntu_version }}:${{ github.sha }}
        dev_release_img_cached: ghcr.io/${{ github.repository }}/dev-release-img-${{ matrix.ubuntu_version }}:buildcache
      run: |
        docker buildx build \
          --load \
          --tag "${dev_release_img}" \
          --cache-from="${dev_release_img_cached}" \
          --build-arg BUILD_ARG=Release \
          -f ${{ matrix.path_dev }} \
          ./edge-ai-libraries-repo/libraries/dl-streamer

    - name: Build deb pkg img using dev release img
      env:
        dev_release_img: ghcr.io/${{ github.repository }}/dev-release-img-${{ matrix.ubuntu_version }}:${{ github.sha }}
      run: |
        docker build \
          --tag ghcr.io/${{ github.repository }}/dls-deb-pkg-${{ matrix.ubuntu_version }}:${{ github.sha }} \
          --build-arg DLSTREAMER_VERSION=${{ env.dlstreamer-version }} \
          --build-arg DLSTREAMER_BUILD_NUMBER=deb-pkg-${{ matrix.ubuntu_version }} \
          --build-arg BASE_IMAGE="${dev_release_img}" \
          -f ${{ matrix.path_deb }} \
          $DLS_PATH

    - name: Extract .deb packages using script
      run: |
        chmod +x $DLS_PATH /scripts/extract_and_verify_debs.sh
        $DLS_PATH /scripts/extract_and_verify_debs.sh ghcr.io/${{ github.repository }}/dls-deb-pkg-${{ matrix.ubuntu_version }}:${{ github.sha }}
        ls
        cp -r deb_packages $DLS_PATH

    - name: Build Docker img from .deb
      run: |
        docker build \
          -t ghcr.io/${{ github.repository }}/dls-deb-${{ matrix.ubuntu_version }}:${{ github.sha }} \
          -f $DLS_PATH /docker/onebinary/${{ matrix.ubuntu_version }}/dlstreamer_${{ matrix.ubuntu_version }}.Dockerfile \
          $DLS_PATH

    # ======================================================== SCANNING PART ========================================================
    - name: Scan Docker image with Trivy
      uses: ./edge-ai-libraries-repo/.github/actions/common/trivy-image-scan
      with:
        image: "ghcr.io/${{ github.repository }}/dls-deb-${{ matrix.ubuntu_version }}:${{ github.sha }}"
        severity: "CRITICAL"
        # output-format: "json"
        output-file: "dls-deb-${{ matrix.ubuntu_version }}-trivy-image-report.txt"

    - name: Upload Trivy report as artifact
      if: always()
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 #4.6.2
      with:
        name: dls-deb-${{ matrix.ubuntu_version }}-trivy-image-report
        path: dls-deb-${{ matrix.ubuntu_version }}-trivy-image-report.txt

    # ======================================================== OH HOST TESTING PART ========================================================
    - name: Download models [TEMPORARY]
      run: |
        export MODELS_PATH=${{ env.MODELS_PATH }}
        $DLS_PATH /samples/download_public_models.sh

    - name: Check models
      run: |
        if [ -d "${{ env.MODELS_PATH }}" ] && [ "$(ls -A "${{ env.MODELS_PATH }}")" ]; then
          echo "Folder "${{ env.MODELS_PATH }}" with models exists and has content."
        else
          echo "Folder "${{ env.MODELS_PATH }}" with models DOES NOT EXIST or is empty."
        fi

    # - name: Check videos
    #   run: |
    #     if [ -d "${{ env.VIDEO_INPUTS_PATH }}" ] && [ "$(ls -A "${{ env.VIDEO_INPUTS_PATH }}")" ]; then
    #       echo "Folder "${{ env.VIDEO_INPUTS_PATH }}" with video inputs exists and has content."
    #     else
    #       echo "Folder "${{ env.VIDEO_INPUTS_PATH }}" with video inputs DOES NOT EXIST or is empty."
    #     fi

    - name: Install DL Streamer on-host
      run: |
        chmod +x $DLS_PATH /tests/scripts/installation-script-entrypoint.sh
        $DLS_PATH /tests/scripts/installation-script-entrypoint.sh $DLS_PATH /deb_packages

    - name: Test hello_dlstreamer script
      run: |
        mkdir -p $DLS_PATH /test_hello_dlstreamer
        cd $DLS_PATH /test_hello_dlstreamer
        export MODELS_PATH=${{ env.MODELS_PATH }}
        echo "Running hello_dlstreamer.sh on CPU with model yolo11s"
        /opt/intel/dlstreamer/scripts/hello_dlstreamer.sh --output=file --device=CPU --model=yolo11s
        OUTPUT_FILE_FOUND=0
        for file in *.mp4; do
            # Check if the file exists and is at least 1kB in size
            if [[ -f "$file" && $(stat --printf="%s" "$file") -ge 1024 ]]; then
                echo "Valid .mp4 file found: $file"
                OUTPUT_FILE_FOUND=1
            fi
        done
        if [[ $OUTPUT_FILE_FOUND -eq 0 ]]; then
            echo "No .mp4 file(s) found or it has less than 1kB size."
            exit 1
        fi

    - name: Upload hello_dlstreamer output video
      uses: actions/upload-artifact@v4.4.0
      if: always()
      with:
        name: hello_dlstreamer_output_video_${{ matrix.ubuntu_version }}_CPU_yolo11s
        path: $DLS_PATH /test_hello_dlstreamer


    - name: Init tests configurations and environment
      run: |
        echo "TODO"

    - name: Run tests
      run: |
        echo "TODO"

    - name: Upload test results
      run: |
        echo "TODO"

    - name: Uninstall dlstreamer
      if: always ()
      run: |
        chmod +x $DLS_PATH /tests/scripts/uninstall-dlstreamer.sh
        $DLS_PATH /tests/scripts/uninstall-dlstreamer.sh

    # ======================================================== CLEAUP PART ========================================================
    - name: Clean up
      if: always()
      run: |
        rm -rf edge-ai-libraries-repo dls-deb-${{ matrix.ubuntu_version }}-trivy-image-report.txt
        docker rmi ghcr.io/${{ github.repository }}/dls-deb-${{ matrix.ubuntu_version }}:${{ github.sha }} || true
